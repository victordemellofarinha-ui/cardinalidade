
//comando para criar tabela
CREATE TABLE nome_da_tabela();

//Comando para ler TODOS os dados da tabela
SELECT * FROM nome_da_tabela;

//Comando para gravar dados
INSERT INTO nome_da_tabela(atributos_da_tabela) VALUES(valores_da_tabela);

//Mudar elementos na tabela
UPDATE nome_da_tabela
SET nome = 'nome_que_voce_quer'
WHERE id = 'id_que_você_quer'

//Adicionar colunas na tabela
ALTER TABLE nome_da_tabela
ADD COLUMN nome_da_coluna VARCHAR(limite_de_caracteres_que_você_quer);

//Apagar uma coluna na tabela
ALTER TABLE detalhes_usuarios
DROP COLUMN nome_da_coluna;

//Juntar duas tabelas
Left join   //pega as informações da tabela da esquerda e o meio
Right join   //pega as informações da tabela da direita e o meio 
Full join   //pega as informações das duas tabelas e junta em uma 
Join ou innerjoin  //pega as informações do meio da tabela

-------------------------------------------------------------------

SELECT 
	coluna1, coluna2 //pode colocar quantas quiser. Isto faz para que apenas estas colunas apareçam quando executa
FROM
	Tabela1 //a primeira tabela é a da esquerda
JOIN
    tabela2
ON //é como se fosse um ligar, você liga tal coluna a outra
    Tabela1.coluna_que_você_quiser = Tabela2.coluna_que_você_quiser
	